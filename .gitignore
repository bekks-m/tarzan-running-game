.env
.env.local
.env.*.local

node_modules/
dist/
dist-ssr/
*.local

.DS_Store
Thumbs.db

test-results/
playwright-report/
blob-report/
playwright/.cache/

coverage/
*.log
npm-debug.log*

.vercel
.vscode/*
!.vscode/extensions.json
.idea/

# Pre-repo revision 2 doc copies, kept for reference only
.archive/

# Reference material from the original game — STUDY ONLY, NEVER COMMITTED.
# IP posture: mechanics are cloned, every shipped asset is original.
# A Disney binary in a public portfolio repo is a licensing problem and
# 30MB of permanent history. Measurements go in original-metrics.md; the
# source material stays local.
reference/tzagdemo/
reference/tzagdemo.zip
reference/**/*.exe
reference/**/*.fsd
reference/**/*.ecm
reference/**/*.mov
reference/**/*.mp4
reference/**/*.mkv
reference/**/*.avi
reference/**/*.webm
