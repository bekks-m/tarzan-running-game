# This project has no backend and needs no secrets at runtime.
# Nothing in here is read by the game. It exists so the shape is documented
# and so .env is gitignored from day one.
#
# Asset-tool keys live in your shell (~/.zshrc), never in the repo:
#   export LUDO_API_KEY="..."
